import React from 'react';
import axios from 'axios';
import AddFrom from '@/components/AddForm';

const Page = async () => {
    return (
        <div>
            <AddFrom />
        </div>
    );
};

export default Page;
